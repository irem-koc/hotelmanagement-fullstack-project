type Props = {};

const Home = (props: Props) => {
  return (
    <div className="min-h-screen flex flex-col">
      {/* Main content area, this will take up the remaining space */}
      <div className="flex-1 bg-gray-100">Home</div>
    </div>
  );
};

export default Home;
